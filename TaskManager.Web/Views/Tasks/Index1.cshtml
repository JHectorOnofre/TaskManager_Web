@model TaskManager.Web.Models.TaskSearchViewModel

<h1>Listado de Tareas</h1> <!-- 12 de Febrero: INDEX1 modificado con vista parcial del _TaskTablePartial -->

<div id="alertContainer"></div>
<button id="btnCrearTask" class="btn btn-success mb-3">
    Crear tarea
</button>

<form id="filterForm" class="row g-3 mb-3">
    <div class="col-md-3">
        <label class="form-label">Texto</label>
        <input asp-for="Text" class="form-control" />
    </div>

    <div class="col-md-2">
        <label class="form-label">Categoría ID</label>
        <input asp-for="CategoryId" class="form-control" />
    </div>

    <div class="col-md-2">
        <label class="form-label">Nombre Categoría</label>
        <input asp-for="CategoryName" class="form-control" />
    </div>

    <div class="col-md-2">
        <label class="form-label">Step</label>
        <input asp-for="Step" class="form-control" />
    </div>

    <div class="col-md-2">
        <label class="form-label">Estado</label>
        <select asp-for="IsCompleted" class="form-select">
            <option value="">--</option>
            <option value="true">Completadas</option>
            <option value="false">Pendientes</option>
        </select>
    </div>

    <div class="col-md-12">
        <button type="button" id="btnBuscarAjax" class="btn btn-primary">Buscar (AJAX)</button>
    </div>
</form>

<div id="taskTableContainer">
    @await Html.PartialAsync("_TaskTablePartial", Model.Result?.Items)
</div>
<!--Declaración de modal-->
<div class="modal fade" id="taskModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content" id="taskModalContent">
            <!-- Aquí se inyectará la partial / Modal de bootstrap oculto-->

        </div>
    </div>
</div>

@section Scripts {
    <script src="~/js/Task/tasks-partial.js"></script>
    <script src="~/js/Task/task-modal.js"></script>
}

@* @model TaskManager.Web.Models.TaskSearchViewModel

@{
    ViewData["Title"] = "Búsqueda de Tareas";
}

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="display-6">Gestión de Tareas</h1>
        <a href="/Tasks/Create" class="btn btn-success">+ Nueva Tarea</a>
    </div>

    <div class="card shadow-sm mb-4">
        <div class="card-body">
            <form asp-action="Search" method="get" class="row g-3">
                <div class="col-md-3">
                    <label class="form-label fw-bold">Texto</label>
                    <input type="text" class="form-control" name="Text" value="@Model?.Text" placeholder="Buscar título..." />
                </div>
                <div class="col-md-3">
                    <label class="form-label fw-bold">Categoría</label>
                    <input type="text" class="form-control" name="CategoryName" value="@Model?.CategoryName" />
                </div>
                <div class="col-md-2">
                    <label class="form-label fw-bold">Estado</label>
                    <select name="IsCompleted" class="form-select">
                        <option value="">-- Todos --</option>
                        <option value="true">Sí</option>
                        <option value="false">No</option>
                    </select>
                </div>
                <div class="col-md-2">
                    <label class="form-label fw-bold">Step</label>
                    <input type="number" class="form-control" name="Step" value="@Model?.Step" />
                </div>
                <div class="col-md-2 d-flex align-items-end">
                    <button type="submit" class="btn btn-primary w-100">Buscar</button>
                </div>
            </form>
        </div>
    </div>

    @if (Model?.Result?.Items != null && Model.Result.Items.Any())
    {
        <div class="table-responsive">
            <table class="table table-hover border">
                <thead class="table-light">
                    <tr>
                        <th>Título</th>
                        <th>Categoría</th>
                        <th>Completado</th>
                        <th>Step</th>
                        <th>Fecha</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var t in Model.Result.Items)
                    {
                        <tr>
                            <td>@t.Title</td>
                            <td><span class="badge bg-info text-dark">@t.CategoryName</span></td>
                            <td>
                                <span class="badge @(t.IsCompleted ? "bg-success" : "bg-warning text-dark")">
                                    @(t.IsCompleted ? "Sí" : "No")
                                </span>
                            </td>
                            <td>@t.Step</td>
                            <td class="text-muted small">@t.CreatedAt.ToString("dd/MM/yyyy")</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <div class="d-flex justify-content-center gap-2 mt-4 pb-5">
            <a class="btn btn-outline-primary @(Model.Page <= 1 ? "disabled" : "")"
               href="@Url.Action("Search", new { Page = Model.Page - 1, Text = Model.Text, CategoryName = Model.CategoryName, IsCompleted = Model.IsCompleted, Step = Model.Step })">
                &laquo; Anterior
            </a>
            <span class="btn border-0 fw-bold">Página @Model.Page</span>
            <a class="btn btn-outline-primary @(Model.Result.Items.Count() < 5 ? "disabled" : "")"
               href="@Url.Action("Search", new { Page = Model.Page + 1, Text = Model.Text, CategoryName = Model.CategoryName, IsCompleted = Model.IsCompleted, Step = Model.Step })">
                Siguiente &raquo;
            </a>
        </div>
    }
    else
    {
        <div class="alert alert-info">No se encontraron resultados para la búsqueda.</div>
    }
</div> *@