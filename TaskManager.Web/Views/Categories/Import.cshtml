@{
    ViewData["Title"] = "Importar categorías desde Excel"; // Formulario e Index
}

<h1>Importar categorías</h1>

<form asp-action="Import" method="post" enctype="multipart/form-data"> <!--el enctype específico es el que nos permite subir archivo (atributo ligado a etiqueta form-->
    <div class="mb-3">
        <label class="form-label">Archivo Excel (.xlsx)</label>
        <input type="file" name="file" class="form-control" accept=".xlsx" /> <!--accept = restringe la carga de archivos a tipo xlsx -->
    </div>

    <button type="submit" class="btn btn-primary">Importar</button>
    <a asp-action="Index" class="btn btn-secondary">Volver al listado</a>
</form>

@if (TempData["Success"] != null)
{
    <div class="alert alert-success mt-3">
        @TempData["Success"]
    </div>
}

@if (TempData["Error"] != null)
{
    <div class="alert alert-danger mt-3">
        @TempData["Error"]
    </div>
}